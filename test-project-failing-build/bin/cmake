#!/usr/bin/env bash
set -euo pipefail

if [ "${1:-}" = "-B" ]; then
  mkdir -p "${2:-build}"
  exit 0
fi

if [ "${1:-}" = "--build" ]; then
  printf 'mock cmake build failed\n' >&2
  exit 2
fi

exit 0
